import styled from 'styled-components';

export const Sheet = styled.div`
  width: 100%;
  height: 100%;
  padding: 20px;

  display: flex;
  justify-content: center;
  align-items: center;

  font-size: 24px;
  text-align: center;

  background-color: #444;
  box-shadow: inset 1px 1px 2px 0 #666, inset -2px -2px 2px 0 #222,
    inset 0 0 150px 0 #000;
`;
