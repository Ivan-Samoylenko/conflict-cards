import { Routes, Route } from "react-router-dom";

function App() {
    return (
        <Routes>
            <Route path='/' element={<div>Hello</div>} />
        </Routes>
    );
};

export default App;